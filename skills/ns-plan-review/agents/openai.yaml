interface:
  display_name: "NS Plan Review"
  short_description: "Red-team plans for implementation readiness"
  default_prompt: "Use $ns-plan-review to independently review, directly harden, and re-review this plan before implementation."

policy:
  allow_implicit_invocation: true
