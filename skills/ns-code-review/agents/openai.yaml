interface:
  display_name: "NS Code Review"
  short_description: "Review, repair, and re-review code changes"
  default_prompt: "Use $ns-code-review to review the current change, repair proven findings locally, and independently re-review until clean or blocked."
policy:
  allow_implicit_invocation: true
