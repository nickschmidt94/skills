interface:
  display_name: "NS Code Review"
  short_description: "Evidence-backed review of code changes"
  default_prompt: "Use $ns-code-review to review the current change for introduced defects and regression risk."
policy:
  allow_implicit_invocation: true
