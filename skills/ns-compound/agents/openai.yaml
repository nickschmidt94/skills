interface:
  display_name: "NS Compound"
  short_description: "Capture durable repository learnings"
  default_prompt: "Use $ns-compound to preserve one verified repository learning from this task."
policy:
  allow_implicit_invocation: false
